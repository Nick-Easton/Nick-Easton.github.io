# 博客维护与更新格式

这份说明用于统一网站更新日志、Git 提交信息和 GitHub Pull Request（PR）说明。无论由 Nick 还是 Codex 修改项目，都按照相同结构记录。

## 1. 一次完整更新应包含什么

每次更新按实际影响检查以下内容：

1. 修改网站文件；
2. 完成与风险相匹配的本地验证；
3. 面向访客的变化写入 `changelog.html`；
4. 功能列表、目录结构或部署方式变化时更新 `README.md`；
5. 使用统一格式创建 Git 提交；
6. 推送后检查 GitHub 和线上站点状态。

纯文字纠错、依赖版本微调等访客几乎无法感知的变化，可以不单独增加更新日志，但仍需要清晰的 Git 提交说明。

## 2. 版本号规则

更新日志使用 `v主版本.功能版本.修订版本`：

- **修订版本**：问题修复、样式微调、文字纠错，例如 `v0.4.0 → v0.4.1`；
- **功能版本**：新增页面、栏目或明显的新功能，例如 `v0.4.1 → v0.5.0`；
- **主版本**：整体架构或设计发生不兼容的大改版，例如 `v0.5.0 → v1.0.0`。

同一批尚未发布的相关改动可以合并到一条版本记录中，避免为很小的连续调整创建大量日志。

## 3. 更新日志格式

新记录放在 `changelog.html` 的列表最上方，包含以下字段：

- 版本号；
- 日期；
- 更新类型；
- 一句话标题；
- 2～5 条访客能够理解的具体变化。

推荐模板：

```html
<li class="changelog-entry">
    <span class="changelog-dot" aria-hidden="true"></span>
    <article>
        <header class="changelog-entry-head">
            <div>
                <span class="version-badge">v0.5.0</span>
                <time datetime="YYYY-MM-DD">YYYY 年 M 月 D 日</time>
            </div>
            <span class="change-type">更新类型</span>
        </header>
        <h3>一句话说明这次更新</h3>
        <ul>
            <li>第一项具体变化。</li>
            <li>第二项具体变化。</li>
        </ul>
    </article>
</li>
```

更新日志强调“网站发生了什么变化”，不要写入密钥、账号、支付信息或只有开发者才能理解的内部细节。

## 4. Git 提交信息格式

提交标题使用：

```text
类型：简短说明
```

可用类型：

- `新增`：新增页面、文章或功能；
- `修复`：修复错误或失效入口；
- `优化`：改善样式、交互、性能或可访问性；
- `文档`：只修改 README、维护说明等文档；
- `部署`：修改托管、工作流或发布配置；
- `重构`：调整代码结构但不改变外部功能。

示例：

```text
新增：加入网站更新日志栏目
修复：显示分类下的全部文章入口
部署：配置 CloudBase 自动发布流程
```

一个提交尽量只表达一个清晰目的。提交正文可补充原因、重要实现和验证结果，但不要重复罗列文件名。

## 5. GitHub PR / Conversation 说明格式

PR 说明固定包含：

1. **改动概述**：为什么要改；
2. **具体改动**：访客能够感知的变化；
3. **验证结果**：测试了哪些页面和交互；
4. **发布影响**：GitHub Pages、CloudBase 或其他部署是否受影响；
5. **文档同步**：更新日志与 README 是否需要更新。

仓库中的 `.github/PULL_REQUEST_TEMPLATE.md` 会在新建 PR 时自动提供这一结构。

## 6. 基础验证清单

提交前至少检查：

- 所有 HTML 页面能够正常打开；
- 新增的本地链接和资源路径存在；
- 桌面与窄屏布局没有明显溢出；
- 导航栏入口在相关页面保持一致；
- JavaScript 没有语法错误；
- `git diff --check` 没有空白字符错误；
- 没有把密钥、令牌、银行卡或个人隐私写入仓库。

推送不等于发布成功。涉及线上更新时，还需要检查实际线上地址和部署任务结果。
